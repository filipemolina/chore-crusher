This directory holds numbered .sql migration files (0001_init.sql, ...),
embedded via embed.FS and applied in order by store.Open. This placeholder
keeps the empty directory tracked in git until the first migration lands.
