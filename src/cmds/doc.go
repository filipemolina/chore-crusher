// Package cmds holds every message type the app passes between components,
// and the tea.Cmds that produce them. See docs/DESIGN.md §7 for the poll-tick
// commands and §3 for the request/response shape every store-backed write
// follows.
package cmds
